@model IEnumerable<MyPupils.Models.PupilStatus>

@{
    ViewData["Title"] = "Index";
}

<h1>Davomat</h1>
    
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Sana)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Id1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Id2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Id3)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Id4)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Id5)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Sana)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Id1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Id2)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Id3)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Id4)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Id5)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
